using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UI_Base : MonoBehaviour
{
    // UI POPUP, UI Scene의 부모가 될 스크립트
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
